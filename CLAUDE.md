# CLAUDE.md

## 会話に出す情報量の方針

このリポジトリでの作業では、会話（トランスクリプト）が長大にならないことを優先する。
デスクトップアプリには表示を一括で畳む機能がないため、書き込む量そのものを抑える。

### コードを会話に貼らない

- ファイルへの変更は Write / Edit ツールで行う。`cat > file <<'EOF'` のようなヒアドキュメントを
  Bash に流し込まない。ヒアドキュメントはコマンド全文が会話に展開されて数百行の塊になる。
- 使い捨てのスクリプト（検証用の Playwright スクリプト等）はスクラッチパッドにファイルとして
  書き、パスを指定して実行する。中身を会話に出さない。
- 応答の中でコードブロックを提示するのは、数行で要点が伝わる場合だけにする。
  それ以上は「どのファイルの何行目をどう変えたか」を文章で説明し、差分ペインに委ねる。

### ファイルを読むとき

- ファイル全体を `cat` しない。`sed -n '120,180p'` のように必要な範囲だけ読む。
- 探索は `grep` / `Grep` で該当行だけを取り出す。全文を出力しない。
- 大きなファイルの構造を掴みたいときは、見出しや関数定義だけを抽出する。

### コマンドの出力

- ビルドやテストのログは `| tail -30` などで絞る。全文を貼らない。
- 失敗したときだけ、原因が分かる範囲を切り出して示す。成功時は結果を一行で報告する。

### 報告の仕方

- 何をしたかは散文で簡潔に述べる。作業ログを時系列で列挙しない。
- 変更内容の確認は差分ペイン（`⇧⌘D`）に任せる前提で書く。
