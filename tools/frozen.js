'use strict';
/* ここまでは並べ替えない、という線。
 *
 * 面の並びを変えると、遊ぶ人にとっては「昨日の第350面」と「今日の第350面」が
 * 別物になる。クリア履歴と自己ベストは面のIDに紐づいているので消えはしないが、
 * 一覧に出てくる順番は変わる。だから、遊ばれた範囲は動かさない。
 *
 *   2026-08-13  第300面まで固定(それ以前から遊ばれていた範囲)
 *   2026-08-17  第400面まで固定(本人の指示)
 *   2026-08-18  第430面まで固定。400で引いたときには気づいていなかったが、
 *               本人は既に第403面をクリアしていた。線の外だったので
 *               並べ替えがその面を第426面へ動かし、間の面が飛ばされた。
 *               線は「遊ばれた一番奥」より必ず先に置くこと
 *
 * 並べ替えの道具は必ずここを読んで、この番号以下には手を出さないこと。
 * 線を動かしてよいのは本人が言ったときだけで、勝手に下げてはいけない。
 */
module.exports = { FROZEN: 430 };
